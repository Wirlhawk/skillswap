import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // images: {
  //   remotePatterns: [new URL('0xctge55estdjuk2.public.blob.vercel-storage.com')]
  // }
};

export default nextConfig;
